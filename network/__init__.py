import sys
import os

# Add the Muscle_wasting directory to the sys.path dynamically
# Get the absolute path of the parent directory of the current file (i.e., network)
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))



